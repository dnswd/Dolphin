# Load the nix flake
use flake

# If a virtual environment exists, activate it
if [ -d .venv ]; then
  export VIRTUAL_ENV="$(pwd)/.venv"
  export PATH="$VIRTUAL_ENV/bin:$PATH"
  echo "Activated virtual environment (.venv) via direnv"
fi
